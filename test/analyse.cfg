process PROD2 = {

        service = MessageLogger {
          untracked vstring destinations = {"cout"}
          untracked PSet cout = { untracked string threshold = "DEBUG" }
          untracked vstring debugModules = {"anal1","anal2"}
        } 

//      service = Timing { }

//      service = SimpleMemoryCheck {
//              untracked int32 ignoreTotal = -1 # default is one
//      } 

        source = PoolSource { 
           untracked vstring fileNames = {'file:HLToutput.root' }
           untracked int32 maxEvents = -1            
        }

        untracked PSet options = {
           untracked bool wantSummary = true  # default is false
        }

// Level-1 Trigger

	module l1extratest = l1extratestMCTruth from "L1Trigger/L1ExtraTestAnalyzer/data/l1extratestMCTruth.cfi"

// HLT

	module anal1 = HLTAnalFilt {
		InputTag inputTag = hlt1
	}

	module anal2 = HLTAnalFilt {
		InputTag inputTag = hlt2
	}

//      module out = EventContentAnalyzer {}

	endpath final = {l1extratest, anal1, anal2}

}
